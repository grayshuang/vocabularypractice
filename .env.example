# ===== 服务端环境变量（云平台后台填写，本地可忽略）=====
# 端口：云平台通常自动注入 PORT，本地默认 3000
PORT=3000

# JWT 签名密钥：生产环境务必改成一段随机长字符串（可用 `openssl rand -base64 32` 生成）
JWT_SECRET=change-this-to-a-long-random-string-please

# 阿里云 DashScope（通义千问）API Key，用于 AI 自动生成题目
# 不填也能用：server.js 里已内置一个默认 key，但建议换成你自己的
DASHSCOPE_API_KEY=
